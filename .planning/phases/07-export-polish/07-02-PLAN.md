---
phase: 07-export-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/sonner.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/app/layout.tsx
  - frontend/src/app/global-error.tsx
  - frontend/src/app/error.tsx
  - frontend/src/app/(dashboard)/error.tsx
  - frontend/src/app/(dashboard)/products/loading.tsx
  - frontend/src/app/(dashboard)/review/loading.tsx
  - frontend/src/app/(dashboard)/clients/loading.tsx
  - frontend/src/app/(dashboard)/dashboard/loading.tsx
autonomous: true

must_haves:
  truths:
    - "Toast notifications appear via Sonner when toast() is called from any client component"
    - "Error boundaries catch runtime errors and show recovery UI with Try Again button"
    - "Loading states show skeleton placeholders instead of blank screens"
  artifacts:
    - path: "frontend/src/components/ui/sonner.tsx"
      provides: "Sonner Toaster component"
      contains: "Toaster"
    - path: "frontend/src/components/ui/skeleton.tsx"
      provides: "Skeleton loading placeholder component"
      contains: "Skeleton"
    - path: "frontend/src/app/global-error.tsx"
      provides: "Global error boundary wrapping root layout"
      contains: "use client"
    - path: "frontend/src/app/(dashboard)/error.tsx"
      provides: "Dashboard-level error boundary"
      contains: "reset"
    - path: "frontend/src/app/(dashboard)/products/loading.tsx"
      provides: "Products page skeleton loader"
      contains: "Skeleton"
    - path: "frontend/src/app/(dashboard)/review/loading.tsx"
      provides: "Review page skeleton loader"
      contains: "Skeleton"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/components/ui/sonner.tsx"
      via: "Toaster component rendered in body"
      pattern: "Toaster"
    - from: "frontend/src/app/(dashboard)/error.tsx"
      to: "reset function"
      via: "Error boundary with retry"
      pattern: "reset"
---

<objective>
Install Sonner toast system and Skeleton components, set up error boundaries and loading skeletons across the app.

Purpose: Establish the UI infrastructure needed for polished UX -- toast notifications for user feedback, error recovery boundaries, and skeleton loading states that replace blank screens.

Output: Working toast system (Sonner), error boundaries at 3 levels (global, root, dashboard), skeleton loading pages for products/review/clients/dashboard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-export-polish/07-CONTEXT.md
@.planning/phases/07-export-polish/07-RESEARCH.md
@frontend/src/app/layout.tsx
@frontend/src/app/(dashboard)/layout.tsx
@frontend/src/components/providers.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Sonner + Skeleton, add Toaster to root layout</name>
  <files>
    frontend/src/components/ui/sonner.tsx
    frontend/src/components/ui/skeleton.tsx
    frontend/src/app/layout.tsx
  </files>
  <action>
    1. Install shadcn components from the frontend directory:
       ```bash
       cd frontend && npx shadcn@latest add sonner --yes
       cd frontend && npx shadcn@latest add skeleton --yes
       ```
       This creates `frontend/src/components/ui/sonner.tsx` and `frontend/src/components/ui/skeleton.tsx` automatically.

    2. Modify `frontend/src/app/layout.tsx` to add the Toaster provider:
       - Import: `import { Toaster } from '@/components/ui/sonner'`
       - Add `<Toaster richColors position="bottom-right" />` inside the `<body>` tag, AFTER the `<Providers>` wrapper (as a sibling, not inside Providers)
       - The final body should look like:
         ```tsx
         <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
           <Providers>
             {children}
           </Providers>
           <Toaster richColors position="bottom-right" />
         </body>
         ```

    Note: The shadcn sonner component wraps the actual Sonner Toaster with theme awareness. Any client component can now call `import { toast } from 'sonner'` to show notifications.
  </action>
  <verify>
    - `frontend/src/components/ui/sonner.tsx` exists
    - `frontend/src/components/ui/skeleton.tsx` exists
    - `frontend/src/app/layout.tsx` includes `<Toaster`
    - `cd frontend && npm run build` succeeds (or at least no import errors)
  </verify>
  <done>Sonner toast system globally available via `import { toast } from 'sonner'`. Skeleton component available for loading states. Toaster rendered in root layout.</done>
</task>

<task type="auto">
  <name>Task 2: Error boundaries + skeleton loading pages</name>
  <files>
    frontend/src/app/global-error.tsx
    frontend/src/app/error.tsx
    frontend/src/app/(dashboard)/error.tsx
    frontend/src/app/(dashboard)/products/loading.tsx
    frontend/src/app/(dashboard)/review/loading.tsx
    frontend/src/app/(dashboard)/clients/loading.tsx
    frontend/src/app/(dashboard)/dashboard/loading.tsx
  </files>
  <action>
    1. Create `frontend/src/app/global-error.tsx` (catches root layout errors):
       - Must be 'use client'
       - Must include `<html>` and `<body>` tags (since it replaces the root layout on error)
       - Props: `{ error: Error & { digest?: string }, reset: () => void }`
       - Show a clean centered error message with "Something went wrong" heading, error.message, and a "Try again" button calling reset()
       - Use minimal inline styles (no component imports since root layout failed)
       - Also show a "Return to Dashboard" link to "/" for navigation recovery

    2. Create `frontend/src/app/error.tsx` (root error boundary inside root layout):
       - Must be 'use client'
       - Import Button from '@/components/ui/button'
       - Centered error display with heading, message, and reset button
       - Add link to dashboard "/" as fallback navigation

    3. Create `frontend/src/app/(dashboard)/error.tsx` (dashboard-level boundary, header stays visible):
       - Must be 'use client'
       - Import Button from '@/components/ui/button', Link from 'next/link'
       - Show error in main content area with heading "Something went wrong", error.message, "Try again" button, and "Go to Dashboard" link
       - Use consistent styling: `flex flex-col items-center justify-center min-h-[400px] gap-4`
       - Text styling: heading text-xl font-semibold text-gray-900, message text-gray-600 max-w-md text-center

    4. Create skeleton loading pages. Each is a default export server component using Skeleton from '@/components/ui/skeleton' and Card from '@/components/ui/card':

       **`frontend/src/app/(dashboard)/products/loading.tsx`:**
       - Page title skeleton (h-9 w-48)
       - Filter bar skeleton (h-10 w-full)
       - 5 product card skeletons, each with: image placeholder (h-12 w-12 rounded), title line (h-4 w-3/4), subtitle line (h-3 w-1/2), status badge (h-6 w-20 rounded-full)

       **`frontend/src/app/(dashboard)/review/loading.tsx`:**
       - Stats bar skeleton (h-10 w-full)
       - Main review area: large content area skeleton (h-64 w-full), sidebar with action buttons (h-10 w-full repeated 3 times)

       **`frontend/src/app/(dashboard)/clients/loading.tsx`:**
       - Page header skeleton (h-9 w-48)
       - Grid of 3 client card skeletons (responsive grid 1/2/3 cols), each with: brand name (h-5 w-2/3), info lines (h-3 w-1/2), badge (h-5 w-16)

       **`frontend/src/app/(dashboard)/dashboard/loading.tsx`:**
       - Welcome message skeleton (h-10 w-64)
       - Subtitle skeleton (h-4 w-48)
       - 2 card skeletons for account info and getting started sections

    All loading pages should use the `space-y-6` wrapper pattern for consistent vertical spacing. Match the layout structure of the actual pages they're replacing.
  </action>
  <verify>
    - All 7 files created and exist:
      * frontend/src/app/global-error.tsx
      * frontend/src/app/error.tsx
      * frontend/src/app/(dashboard)/error.tsx
      * frontend/src/app/(dashboard)/products/loading.tsx
      * frontend/src/app/(dashboard)/review/loading.tsx
      * frontend/src/app/(dashboard)/clients/loading.tsx
      * frontend/src/app/(dashboard)/dashboard/loading.tsx
    - `cd frontend && npx tsc --noEmit` passes (or `npm run build` succeeds)
  </verify>
  <done>3-level error boundary hierarchy (global, root, dashboard) catches errors at appropriate levels with recovery UI. 4 skeleton loading pages provide content-shaped placeholders during page transitions for products, review, clients, and dashboard pages.</done>
</task>

</tasks>

<verification>
- Sonner Toaster renders in root layout
- Error boundaries exist at global, root, and dashboard levels
- Loading skeletons exist for all main dashboard pages
- All files compile without TypeScript errors
</verification>

<success_criteria>
1. `import { toast } from 'sonner'` works from any client component and shows notifications
2. Error in dashboard page shows dashboard error boundary (header stays visible) with Try Again button
3. Products, review, clients, and dashboard pages show skeleton loaders during navigation
4. Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-export-polish/07-02-SUMMARY.md`
</output>
