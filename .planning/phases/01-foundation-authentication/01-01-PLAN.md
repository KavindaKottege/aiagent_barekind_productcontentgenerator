---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - frontend/package.json
  - frontend/src/app/layout.tsx
  - frontend/.env.local
  - frontend/.env.example
  - backend/app/main.py
  - backend/app/config.py
  - backend/requirements.txt
  - backend/.env
  - backend/.env.example
autonomous: true

must_haves:
  truths:
    - "Docker Compose starts PostgreSQL with single command"
    - "Next.js dev server runs on port 3000"
    - "FastAPI dev server runs on port 8000 with /docs endpoint"
    - "Backend connects to PostgreSQL successfully"
  artifacts:
    - path: "docker-compose.yml"
      provides: "PostgreSQL + pgAdmin containers"
      contains: "postgres:16"
    - path: "frontend/package.json"
      provides: "Next.js 15 with TypeScript and Tailwind"
      contains: "next"
    - path: "backend/app/main.py"
      provides: "FastAPI app with CORS configured"
      contains: "FastAPI"
    - path: "backend/app/database.py"
      provides: "Async SQLAlchemy engine and session"
      contains: "create_async_engine"
  key_links:
    - from: "backend/app/main.py"
      to: "docker-compose.yml"
      via: "DATABASE_URL environment variable"
      pattern: "postgresql\\+asyncpg"
    - from: "frontend"
      to: "backend"
      via: "CORS configuration"
      pattern: "localhost:3000"
---

<objective>
Set up the complete development environment with Docker Compose for PostgreSQL, Next.js 15 frontend, and FastAPI backend with async database connectivity.

Purpose: Establish the foundational infrastructure that all subsequent phases build upon. This is the "dev mode just works" setup from CONTEXT.md.

Output: Running development environment with Next.js on :3000, FastAPI on :8000, PostgreSQL on :5432, and pgAdmin on :5050.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Docker Compose for PostgreSQL development</name>
  <files>
    docker-compose.yml
  </files>
  <action>
Create `docker-compose.yml` at project root with:

1. PostgreSQL 16 service:
   - Container name: dev_postgres
   - Environment: POSTGRES_USER=devuser, POSTGRES_PASSWORD=devpassword, POSTGRES_DB=saas_dev
   - Port: 5432:5432
   - Named volume for data persistence (postgres_data)
   - Health check using pg_isready

2. pgAdmin service:
   - Container name: dev_pgadmin
   - Port: 5050:80
   - Default credentials: admin@example.com / admin
   - Depends on postgres service

Use pattern from 01-RESEARCH.md Docker Compose section.
  </action>
  <verify>
Run `docker-compose up -d` and verify:
- `docker-compose ps` shows both containers running
- `docker-compose logs postgres` shows "database system is ready"
- pgAdmin accessible at http://localhost:5050
  </verify>
  <done>
PostgreSQL and pgAdmin containers start successfully with single `docker-compose up -d` command.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js 15 frontend with App Router</name>
  <files>
    frontend/package.json
    frontend/src/app/layout.tsx
    frontend/src/app/page.tsx
    frontend/.env.local
    frontend/.env.example
    frontend/tailwind.config.ts
    frontend/tsconfig.json
  </files>
  <action>
Create Next.js 15 application in `frontend/` directory:

1. Run: `npx create-next-app@latest frontend --typescript --tailwind --app --src-dir --use-npm --no-import-alias`

2. Install additional dependencies:
   `cd frontend && npm install zod jose`

3. Create `.env.local`:
   ```
   SESSION_SECRET=dev-secret-key-change-in-production-32chars
   NEXT_PUBLIC_API_URL=http://localhost:8000
   ```

4. Create `.env.example` (commit this):
   ```
   SESSION_SECRET=generate-with-openssl-rand-hex-32
   NEXT_PUBLIC_API_URL=http://localhost:8000
   ```

5. Update `src/app/page.tsx` with simple placeholder:
   ```tsx
   export default function Home() {
     return (
       <main className="flex min-h-screen flex-col items-center justify-center p-24">
         <h1 className="text-4xl font-bold">Product Content Generator</h1>
         <p className="mt-4 text-gray-600">Setting up...</p>
       </main>
     )
   }
   ```

Keep default layout.tsx but ensure metadata has proper title "Product Content Generator".
  </action>
  <verify>
Run `cd frontend && npm run dev` and verify:
- Dev server starts on http://localhost:3000
- Page loads without errors showing "Product Content Generator"
- No TypeScript errors in console
  </verify>
  <done>
Next.js 15 frontend scaffold running with TypeScript, Tailwind, App Router, and environment configured for API communication.
  </done>
</task>

<task type="auto">
  <name>Task 3: Scaffold FastAPI backend with async SQLAlchemy</name>
  <files>
    backend/requirements.txt
    backend/app/__init__.py
    backend/app/main.py
    backend/app/config.py
    backend/app/database.py
    backend/.env
    backend/.env.example
  </files>
  <action>
Create FastAPI application in `backend/` directory:

1. Create `backend/requirements.txt`:
   ```
   fastapi[standard]>=0.115.0
   sqlalchemy[asyncio]>=2.0.0
   asyncpg>=0.29.0
   alembic>=1.13.0
   pwdlib[argon2]>=0.2.0
   pyjwt>=2.8.0
   python-multipart>=0.0.6
   uvicorn[standard]>=0.30.0
   pydantic-settings>=2.0.0
   ```

2. Create `backend/app/config.py` using Pydantic Settings:
   - DATABASE_URL (default: postgresql+asyncpg://devuser:devpassword@localhost:5432/saas_dev)
   - SECRET_KEY (default: dev-secret-key-change-in-production-32chars)
   - ALGORITHM (default: HS256)
   - ACCESS_TOKEN_EXPIRE_MINUTES (default: 10080 = 7 days)
   - FRONTEND_URL (default: http://localhost:3000)
   - ENVIRONMENT (default: development)

3. Create `backend/app/database.py`:
   - Async engine with create_async_engine
   - Connection pooling: pool_size=10, max_overflow=20, pool_pre_ping=True
   - async_sessionmaker with expire_on_commit=False (critical for async)
   - get_db dependency generator

4. Create `backend/app/main.py`:
   - FastAPI app with title "Product Content Generator API"
   - CORS middleware allowing frontend origin with credentials
   - Health check endpoint at /health returning {"status": "ok"}
   - Root endpoint returning {"message": "Product Content Generator API"}

5. Create `backend/.env` with dev defaults (matching config.py defaults)

6. Create `backend/.env.example` (commit this) with placeholder values

7. Create empty `backend/app/__init__.py`

Use patterns from 01-RESEARCH.md (Pattern 5: Async SQLAlchemy Session Management, Pattern 6: CORS Configuration).
  </action>
  <verify>
Run:
1. `cd backend && python -m venv .venv && source .venv/bin/activate`
2. `pip install -r requirements.txt`
3. `uvicorn app.main:app --reload`

Verify:
- FastAPI docs at http://localhost:8000/docs
- Health check at http://localhost:8000/health returns {"status": "ok"}
- No import errors on startup
  </verify>
  <done>
FastAPI backend running with async SQLAlchemy configured, CORS enabled for frontend, and health check endpoint responding.
  </done>
</task>

</tasks>

<verification>
1. Docker: `docker-compose up -d && docker-compose ps` shows healthy containers
2. Frontend: `cd frontend && npm run dev` serves app on :3000
3. Backend: `cd backend && uvicorn app.main:app --reload` serves API on :8000
4. Integration: Frontend can reach backend (no CORS errors in browser console when accessing :8000 from :3000)
5. Database: Backend health check confirms database connectivity (or logs show successful engine creation)
</verification>

<success_criteria>
- [ ] Single `docker-compose up -d` starts PostgreSQL and pgAdmin
- [ ] `npm run dev` in frontend/ starts Next.js on port 3000
- [ ] `uvicorn app.main:app` in backend/ starts FastAPI on port 8000
- [ ] FastAPI /docs endpoint accessible and shows API documentation
- [ ] No CORS errors when frontend makes requests to backend
- [ ] All environment variables documented in .env.example files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
